# next-notion-blog

## Package manager

This project uses **pnpm**. Always use `pnpm` for package installation and management — never `npm` or `yarn`.

## After making changes

Always run `pnpm tsc --noEmit` to verify there are no type errors before committing.
